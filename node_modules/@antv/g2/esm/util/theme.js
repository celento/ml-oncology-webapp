import { deepMix, isObject } from '@antv/util';
import { getTheme } from '../theme';
/**
 * @ignore
 * 合并成行的 theme 主题配置
 * @param themeObject
 * @param theme
 */
export function mergeTheme(themeObject, theme) {
    var newThemeObject = isObject(theme) ? theme : getTheme(theme);
    return deepMix(themeObject, newThemeObject);
}
//# sourceMappingURL=theme.js.map