import { LooseObject, StyleSheet } from '../interface';
/**
 * 获取主题配置信息。
 * @param theme 主题名
 */
export declare function getTheme(theme?: string): LooseObject;
/**
 * 注册新的主题配置信息。
 * @param theme 主题名。
 * @param value 具体的主题配置。
 */
export declare function registerTheme(theme: string, value: LooseObject | string): void;
/**
 * 获取对应主题样式表
 * @param name 样式表名
 */
export declare function getStyleSheet(name?: string): StyleSheet;
/**
 * 注册主题样式表
 * @param name 样式表名
 * @param styleSheet 样式表定义
 */
export declare function registerStyleSheet(name: string, styleSheet: StyleSheet): void;
