import { BBox, IGroup, IShape } from '../../../dependents';
/**
 * @ignore
 * 根据图形元素以及 label 的 bbox 进行调整，如果 label 超出了 shape 的 bbox 则不展示
 */
export declare function limitInShape(labels: IGroup[], shapes: IShape[] | IGroup[], items: any, region: BBox): void;
